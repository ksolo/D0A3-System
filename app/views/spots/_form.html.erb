<div class="row">
  <div class="span6 offset3">
	<%= form_for([@group, @spot]) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	  <p>
	    <%= f.label "Niño" %>
		<%= select("spot", "child_id", Person.all.collect {|p| [ p.name, p.id ] }, { include_blank: 'Selecciona'}) %> <br><br>
	  </p>
	  <p>
	    <%= f.label "Cuidador" %>
		<%= select("spot", "tutor_id", Person.all.collect {|p| [ p.name, p.id ] }, { include_blank: 'Selecciona'}) %> <br><br>
	  </p>
	  <p>
	    <%= f.submit "Guardar", class: "btn btn-sm btn-primary" %>
	  </p>
	<% end %>
  </div>
</div>
<!-- spot.child.family_relations -->